# Deep-Koopman 核心依赖（训练 / 评估 / MPC）
torch>=2.0
numpy>=1.24
matplotlib>=3.7
pyyaml>=6.0
scipy>=1.10

# 代价函数分析（scripts/analyze_v4_cost.py：Python 侧镜像 C++ 潜空间 QP 做对照实验）
osqp>=0.6

# PT → ONNX 导出与精度验证（C++ MPC 部署）
onnx>=1.16
onnxruntime>=1.17
onnxscript>=0.1

# 数据处理（rosbag 切分，需 ROS 环境）
# rosbag
